'use strict';

/**
 * competition-success service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::competition-success.competition-success');
